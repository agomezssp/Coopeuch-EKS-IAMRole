apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: iamrole-sqs
  name: iamrole-sqs
  namespace: 3htp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iamrole-sqs
  template:
    metadata:
      labels:
        app: iamrole-sqs
        version: ""
    spec:
      containers:
      - args:
        - java -jar /app/iamrole-sqs-0.0.1.jar --spring.config.location=/app/application.properties
        command:
        - /bin/sh
        - -c
        image: 369595298303.dkr.ecr.us-east-2.amazonaws.com/coopeuch-iamrole-java-test:1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          protocol: TCP
        resources:
          limits:
            cpu: 300m
            memory: 512Mi
          requests:
            cpu: 200m
            memory: 256Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /app/application.properties
          name: conf-volumen
          subPath: application.properties
        imagePullPolicy: IfNotPresent
        name: iamrole-sqs
        ports:
        - containerPort: 8080
          protocol: TCP
      restartPolicy: Always
      serviceAccount: ccm-serviceaccount
      serviceAccountName: ccm-serviceaccount
      schedulerName: default-scheduler
      volumes:
      - configMap:
          items:
          - key: application.properties
            path: application.properties
          name: cm-sqs
        name: conf-volumen